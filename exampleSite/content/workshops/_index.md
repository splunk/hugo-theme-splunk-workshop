+++
title = "Workshops"
description = "Pick a workshop and dive in. Each one is built to be completed in a single sitting."
layout = "hero"
# Each child of /workshops/ is itself a section bundle (one workshop). Set
# `subsections = true` so the auto-card grid lists the sub-sections (one card
# per workshop) instead of the section's regular pages.
subsections = true
+++
