+++
title       = "Exercise archetype"
description = "What `hugo new --kind exercise` produces — hands-on steps, terminals, and a hidden solution."
weight      = 40
time        = "15 min"
difficulty  = "beginner"
tags        = ["sample", "archetype"]
+++

{{< lead >}}
What the reader will build in this exercise. State the end-state clearly so they know when they're done — "you'll have a working `curl` against the API returning 200, and you'll be able to read the response body" is much more useful than "you'll learn about HTTP."
{{< /lead >}}

This page was generated from the `exercise` archetype. If you want the page to drop out of the prev/next pager chain (a useful pattern when an exercise is launched from a parent lesson via a button rather than read linearly), add `nopager = true` to its front matter — see [Pager override demo › nopager](/workshops/pager-demo/06-nopager/) for a live example of the behaviour.

## Generated by

```bash
hugo new --kind exercise content/workshops/getting-started/03-build-a-query.md
```

## Prerequisites

{{< prerequisites >}}
- A working Splunk install — see [Installing Splunk](/workshops/getting-started/02-installation/) if you haven't done it yet.
- Sample data loaded into the `main` index.
{{< /prerequisites >}}

## Steps

{{< step "Set up the workspace" >}}
Brief instruction for the first step. Anchor the reader in space — what directory, what file, what tab. Specificity beats elegance.

```bash
mkdir my-exercise && cd my-exercise
```
{{< /step >}}

{{< step "Run the example" >}}
Walk through the core action. Show the expected output inline so the reader can compare their result to ground truth without leaving the page:

{{< terminal >}}
$ ./run.sh
Hello, workshop.
{{< /terminal >}}

If their output differs, that's a debugging exercise — and that's *good*. Workshops learn by hitting expected friction.
{{< /step >}}

{{< step "Verify your result" >}}
What success looks like. If there's a self-verifiable criterion, list it: "the file `output.txt` should contain three lines, the last one starting with `OK`".

The `solution` shortcode hides the expected end-state behind a click so readers try first:

{{< solution >}}
The file `output.txt` contains:

```text
RUN 1: started
RUN 1: completed
OK: 1 of 1 tests passed
```

If your line 3 starts with anything other than `OK`, re-run the test in verbose mode (`./run.sh -v`) and check the parser output.
{{< /solution >}}
{{< /step >}}

{{< checkpoint "You've completed the exercise" >}}

The `checkpoint` shortcode marks a milestone the reader can self-verify against. It's the visual "you can move on now" signal. Pair it with a clear link back to the parent lesson:

→ Back to [Your First Search](/workshops/getting-started/03-first-search/)
