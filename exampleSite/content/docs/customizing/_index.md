+++
title       = "Customizing"
linkTitle   = "Customizing"
description = "Rebrand the theme without touching CSS — every visual choice is a Hugo param."
weight      = 20
layout      = "chapter"
subtitle    = "Chapter · Brand"
+++

The theme's visual identity is defined entirely in `hugo.toml` under `[params]`. That means you can change colors, fonts, logos, and layout toggles without ever editing a CSS file. The chapters that follow walk through each surface.
